spack:
  definitions:
  - packages:
    - cmake@3.21.4
  specs:
  - matrix:
    - [$packages]
    - ['%gcc@7.5.0']
  packages:
# double colon in the next line, so that existing externals are ignored
    cmake::
      buildable: true
  view: false
