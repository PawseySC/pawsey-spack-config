spack:
  packages:
# double colon in the next line, so that existing externals are ignored
    cmake::
      buildable: true
  definitions:
  - packages:
    - cmake@3.21.4
  specs:
  - matrix:
    - [$packages]
    - ['%gcc@10.3.0']
# TODO: enable target
#    - ['target=zen2']
  view: false
# TODO: list as external in packages.yaml
# TODO: write module projections
# TODO: module as core module
