spack:
  packages:
# double colon in the next line, so that existing externals are ignored
    cmake::
      buildable: true
  definitions:
  - packages:
    - cmake@3.21.4
  specs:
  - matrix:
    - [$packages]
    - ['%gcc@10.3.0']
# TODO: enable target, ensure listing as external in packages.yaml
#    - ['target=zen2']
  view: false
