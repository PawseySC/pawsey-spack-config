config:

  # This is the path to the root of the Spack install tree.
  install_tree:
    root: /software/$PAWSEY_PROJECT/$USER/setonix/software

  # Locations where templates should be found
  template_dirs:
# this one should only be used by users
    - /software/$PAWSEY_PROJECT/$USER/setonix/templates_setonix
# Spack user (Pawsey staff) should populate this one
    - /sofware/setonix/2022.01/setonix/pawsey-spack-config/setonix/templates_setonix

  # Temporary locations Spack can try to use for builds.
  # See recommended options in defaults/config.yaml
  build_stage:
#    - $tempdir/$user/spack-stage
    - /software/$PAWSEY_PROJECT/$USER/setonix/software/build_stage

  # Directory in which to run tests and store test results.
  test_stage: /software/$PAWSEY_PROJECT/$USER/setonix/software/test_stage

  # Cache directory for already downloaded source tarballs and archived
  # repositories. This can be purged with `spack clean --downloads`.
  source_cache: /software/$PAWSEY_PROJECT/$USER/setonix/software/source_cache


  # Cache directory for miscellaneous files, like the package index.
  # This can be purged with `spack clean --misc-cache`
  misc_cache: /software/$PAWSEY_PROJECT/$USER/setonix/software/misc_cache

  # concretizer: `clingo` or `original` (good for debugging concretisations)
  concretizer: clingo
  #concretizer: original
