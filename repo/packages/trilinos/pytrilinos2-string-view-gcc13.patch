--- a/packages/PyTrilinos2/scripts/PyTrilinos2_config.cfg
+++ b/packages/PyTrilinos2/scripts/PyTrilinos2_config.cfg
@@ -23,6 +23,11 @@
 -class std::iterator
 -class std::reverse_iterator
 -class std::set
+# GCC13/libstdc++ can end up with binder-generated wrappers for
+# std::basic_string_view and direct inclusion of <bits/string_view.tcc>,
+# which breaks generated translation units. Keep the type handled via
+# pybind11's STL casters and avoid internal libstdc++ headers.
+-class std::basic_string_view
+-include <bits/string_view.tcc>
 
 # OpenMPI
 -class ompi_status_public_t
